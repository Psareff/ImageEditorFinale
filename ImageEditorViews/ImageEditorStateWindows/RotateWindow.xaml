<Window x:Class="ImageEditorFinale.RotateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageEditorFinale"
        mc:Ignorable="d"
        Title="RotateWindow" Height="100" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Angle: "/>
            <TextBox Name="AngleSetter" Width="50"/>
        </StackPanel>
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <RadioButton Name="ClockWise" Content="Clockwise" Margin="0,5,0,5"/>
            <RadioButton Name="CounterClockWise" Content="CClockwise" Margin="0,5,0,5"/>
        </StackPanel>
        <StackPanel Grid.Column="2" VerticalAlignment="Center">
            <Button Name="Save" Content="Save" Margin="0,5,0,5" Width="50"/>
            <Button Name="Cancel" Content="Cancel" Margin="0,5,0,5" Width="50"/>
        </StackPanel>
    </Grid>
</Window>
